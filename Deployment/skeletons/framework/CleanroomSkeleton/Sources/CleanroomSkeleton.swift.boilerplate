//
//  ${project:name}.swift
//  Cleanroom Project
//
//  Created by ${project:creator:name} on ^formatDate(^currentTime()|M/d/yy).
//  Copyright © ^formatDate(^currentTime()|yyyy) Gilt Groupe. All rights reserved.
//

public protocol ${project:name}
{
    // this is a dummy protocol; it ensures that a
    // Swift header is generated (needed to compile)
}

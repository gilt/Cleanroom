PRODUCT_NAME = $project:name
PRODUCT_BUNDLE_IDENTIFIER = ${project:bundle-prefix}.$$(PRODUCT_NAME:rfc1034identifier)
INFOPLIST_FILE = BuildControl/Info-Framework.plist
DYLIB_CURRENT_VERSION = $$(CURRENT_PROJECT_VERSION)
^if($project:settings[app-extension-only]|APPLICATION_EXTENSION_API_ONLY = YES
)
^if($project:settings[bridging-header]|SWIFT_OBJC_BRIDGING_HEADER = $project:settings[bridging-header]
)